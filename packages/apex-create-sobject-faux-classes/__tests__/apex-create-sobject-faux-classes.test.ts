import * as mocha from 'mocha';
import { expect } from 'chai';
import { activate } from '../src';

describe('apex-create-sobject-faux-classes', () => {
  it('needs tests', () => {
    // activate();
    expect(true).to.be.true;
  });
});
